const MULTIPLICAND = 9,
  MULTIPLIER = 9;

for (let m7cand = 1; m7cand <= MULTIPLICAND; m7cand++) {
  for (let m5plier = 1; m5plier <= MULTIPLIER; m5plier++) {
    const product = m7cand * m5plier;
    console.log(`${m7cand} x ${m5plier} = ${product}`);
  }
}
